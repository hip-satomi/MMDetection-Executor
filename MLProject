name: MMDetection-Executor

conda_env: conda.yaml
# Can have a docker_env instead of a conda_env, e.g.
# docker_env:
#    image:  mlflow-docker-example

entry_points:
  main:
    parameters:
      input_image: path
      config: path
      checkpoint: path
    command: "python main.py --image input_image --config {config} --checkpoint {checkpoint}"

